# RepoMedic - Environment Variables Example

# OPTIONAL: GitHub Personal Access Token (PAT) to bypass the default 60 req/hour rate limit
# Generate a token with no scopes (fine-grained or classic) at https://github.com/settings/tokens
VITE_GITHUB_TOKEN=your_github_personal_access_token_here
